import { POSITION } from "vue-toastification";

export const ROOT_API = "http://localhost:3001";
export const HISTORY_SKIP_COUNT = 25;
export const TOAST_CONFIG = { position: POSITION.BOTTOM_RIGHT, containerClassName: "app-toast" };
